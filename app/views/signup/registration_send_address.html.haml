.signup-container
  = render 'registration-send-address-header'
  .signup-registration-main
    %h2.signup-registration-main__head
      商品送付先情報
    .signup-registration-main__inner
      = form_with model: @send_address, local: true, url: signup_index_path, method: 'POST', class: '' do |f|
        .field
          .field__label
            = f.label 'お名前'
            %span.field__label__form-require 必須
          .field__input
            = f.text_field :family_name, placeholder: '例）山田',autofocus: true
          .field__input
            = f.text_field :first_name, placeholder: '例）彩', class:'field__input__send-name'
        .field
          .field__label
            = f.label 'お名前カナ'
            %span.field__label__form-require 必須
          .field__input
            = f.text_field :family_name_kana, placeholder: '例）ヤマダ'
          .field__input
            = f.text_field :first_name_kana, placeholder: '例）アヤ', class:'field__input__send-name'
        .field
          .field__label
            = f.label '郵便番号'
            %span.field__label__form-require 必須
          .field__input
            = f.text_field :zip_code, placeholder: '例）123-4567'
        .field
          .field__label
            = f.label '都道府県'
            %span.field__label__form-require 必須
          .field__input
            = f.select :prefecture, options_for_select(DeliverAddress.prefectures.keys)
        .field
          .field__label
            = f.label '市区町村'
            %span.field__label__form-require 必須
          .field__input
            = f.text_field :city, placeholder: '例）横浜市緑区'
        .field
          .field__label
            = f.label '番地'
            %span.field__label__form-require 必須
          .field__input
            = f.text_field :address1, placeholder: '例）青山1-1-1'
        .field
          .field__label
            = f.label '建物名'
            %span.field__label__form-arbitrary 任意
          .field__input
            = f.text_field :address2, placeholder: '例）柳ビル103'
        .field
          .field__label
            = f.label '電話'
            %span.field__label__form-arbitrary 任意
          .field__input
            = f.text_field :phone_number, placeholder: '例）09012345678'
        .field
          = f.submit "登録する", class: 'red-btn'
    .signup-index-main__inner__announce
      %p
        登録することで、
        = link_to '利用規約', '#' 
        と
        = link_to 'プライバシーポリシー', '#'
        に
        %br
        同意したものとみなされます。
  =render 'footer'